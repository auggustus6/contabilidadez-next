import { styled } from "@/styles";

export const Wrapper = styled("footer", {
  width: "100%",
  height: "$40",
  background: "#333",
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  color: "White",
});
