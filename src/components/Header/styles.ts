import { styled } from "@/styles";

export const Wrapper = styled("header", {
  width: "100%",
  height: "$20",
  background: "#ddd",
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});
