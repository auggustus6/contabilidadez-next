import { ReactNode } from "react";
import * as Styles from './styles'
import type { VariantProps } from "@stitches/react";

type Variants = VariantProps<typeof Styles.{{pascalCase name}}Wrapper>;
interface {{pascalCase name}}Props extends Variants {
  children?: ReactNode;
  className?: string;
}

const {{pascalCase name}} = ({children, className, ...variantsAndRest}:{{pascalCase name}}Props) => {
  return(
    <Styles.{{pascalCase name}}Wrapper className={className} {...variantsAndRest}>
      {children}
    </Styles.{{pascalCase name}}Wrapper>
  )
};

export { {{pascalCase name}} }